% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tbl_statistics.R
\name{degenerate_prop_count_wide}
\alias{degenerate_prop_count_wide}
\title{Produce a proper tabulation when the \code{var_name} of interest is degenerate,
i.e. contains no relevant rows, either truly containing 0 rows or having
only NA values when \code{include_missing} is \code{FALSE}}
\usage{
degenerate_prop_count_wide(df, comb, var_name)
}
\arguments{
\item{df}{the data.frame}

\item{comb}{the subset of columns to key rows by for this combination}

\item{var_name}{the prop_count column name, used as-is (no prefix)}
}
\value{
a tibble with one column named \code{var_name} (all \code{NA}), one row
per distinct \code{comb} tuple present in \code{df} (or exactly one row if \code{comb}
is empty, matching \code{\link[dplyr:group_by]{dplyr::group_by()}}'s zero-columns convention)
}
\description{
Produce a proper tabulation when the \code{var_name} of interest is degenerate,
i.e. contains no relevant rows, either truly containing 0 rows or having
only NA values when \code{include_missing} is \code{FALSE}
}
