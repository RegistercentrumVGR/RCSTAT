% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aggregate_table_one.R
\name{prettify_table_one}
\alias{prettify_table_one}
\title{Prettify the result of \code{\link[=table_one]{table_one()}}}
\usage{
prettify_table_one(df, vars = NULL, blank_repeated = TRUE)
}
\arguments{
\item{df}{the result of \code{\link[=table_one]{table_one()}}}

\item{vars}{a data.frame with \code{ColumnName} and \code{Description} columns
(the same shape used by \code{\link[=decode_names]{decode_names()}}) used to recode \code{variable} into
human-readable labels with \code{\link[dplyr:case_match]{dplyr::case_match()}}. Variables not found in
\code{vars} are left unchanged}

\item{blank_repeated}{whether or not to blank \code{"Variabel"} on rows
where it repeats the value directly above it}
}
\value{
a data.frame
}
\description{
Renames \code{variable}/\code{category}/\code{value} to \code{"Variabel"}/\code{"Utfall"}/
\code{"Värde"}, optionally recodes variable identifiers into
human-readable labels, and blanks \code{"Variabel"} on rows where it repeats
the value directly above it, so a categorical variable's name is only
printed once instead of on every category row.
}
