% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aggregate_table_one.R
\name{table_one}
\alias{table_one}
\title{Create a "table one" style summary table}
\usage{
table_one(
  df,
  vars,
  median_vars = NULL,
  include_missing,
  max_categories = 15,
  digits = 1,
  obfuscate_data = TRUE,
  group_cols = NULL,
  marginal_cols,
  value_labels = NULL
)
}
\arguments{
\item{df}{a data.frame}

\item{vars}{the variables to summarize}

\item{median_vars}{a subset of the numeric variables in \code{vars} that
should be summarized as median (5\%-95\%) instead of mean (SD)}

\item{include_missing}{whether or not missing values should be included
when calculating proportions, passed to \code{\link[=get_aggregate_value]{get_aggregate_value()}}}

\item{max_categories}{the maximum number of categories a categorical
variable can have before the smallest categories are lumped into
\code{"Other"}}

\item{digits}{the number of decimals used when formatting statistics}

\item{obfuscate_data}{whether or not to obfuscate data}

\item{group_cols}{the columns to group by}

\item{marginal_cols}{a subset of \code{group_cols} for which to marginally
summarize, passed to \code{\link[=get_aggregate_value]{get_aggregate_value()}}}

\item{value_labels}{an optional data.frame with \code{ColumnName}, \code{ValueCode},
and \code{ValueName} columns (the same shape used by \code{\link[=decode_data]{decode_data()}}), used to
decode categorical variables in \code{vars} before summarizing. Classification
of variables into binary/categorical/numeric always runs on the raw,
undecoded data first, so decoding a binary variable's value labels never
changes its classification. If \code{NULL} (the default), categorical values
are left undecoded}
}
\value{
a data.frame with one row per variable/category and one column
per \code{group_cols} combination. \code{category} is \code{" "} (a single whitespace)
for rows that are not categorical, so the table renders without \code{NA}
}
\description{
Summarizes a set of variables by type (binary, categorical, or numeric)
and pivots the result into a wide, printable table with one column per
\code{group_cols} combination (mirroring \code{\link[=get_aggregate_value]{get_aggregate_value()}}, including
its marginal "Alla"/"Riket" totals).
Variables are classified automatically:
\itemize{
\item \code{logical} columns, and numeric columns only containing \code{0}/\code{1} (and
\code{NA}), are treated as binary and summarized as N (\%) on a single row.
\item Remaining \code{character}/\code{factor} columns are treated as categorical and
summarized as N (\%) with one row per category. Categories are lumped
with \code{\link[forcats:fct_lump]{forcats::fct_lump_n()}} when there are more than \code{max_categories}
of them.
\item Remaining numeric columns are treated as continuous and summarized as
mean (SD), unless listed in \code{median_vars}, in which case they are
summarized as median (5\%-95\%).
}
}
